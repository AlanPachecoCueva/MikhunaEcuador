@model IEnumerable<MikhunaEcuador.Models.Pasos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Paso)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Paso)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PasosID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PasosID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PasosID })
        </td>
    </tr>
}

</table>
